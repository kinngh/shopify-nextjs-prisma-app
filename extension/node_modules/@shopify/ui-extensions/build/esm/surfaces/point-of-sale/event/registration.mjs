function createExtensionRegistrationFunction() {
  const extensionWrapper = (target, callback) => {
    var _shopify;
    (_shopify = globalThis.shopify) === null || _shopify === void 0 ? void 0 : _shopify.extend(target, callback);
    return callback;
  };
  return extensionWrapper;
}

export { createExtensionRegistrationFunction };

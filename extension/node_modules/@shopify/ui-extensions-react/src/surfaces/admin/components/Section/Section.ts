import {Section as BaseSection} from '@shopify/ui-extensions/admin';
import {createRemoteReactComponent} from '@remote-ui/react';

export const Section = createRemoteReactComponent(BaseSection);
export type {SectionProps} from '@shopify/ui-extensions/admin';

'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var api = require('./api.js');
var subscription = require('./subscription.js');

/**
 * Returns the cart instructions used to create the checkout and possibly limit extension capabilities.
 */
function useInstructions() {
  return subscription.useSubscription(api.useApi().instructions);
}

exports.useInstructions = useInstructions;

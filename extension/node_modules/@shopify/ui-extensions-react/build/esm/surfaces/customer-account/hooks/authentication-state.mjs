import { useSubscription } from './subscription.mjs';
import { useApi } from './api.mjs';

/**
 * Returns authentication state of Order status page.
 */
function useAuthenticationState() {
  return useSubscription(useApi().authenticationState);
}

export { useAuthenticationState };

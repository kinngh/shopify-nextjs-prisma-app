import { CameraScanner as CameraScanner$1 } from '@shopify/ui-extensions/point-of-sale';
import { createRemoteReactComponent } from '@remote-ui/react';

const CameraScanner = createRemoteReactComponent(CameraScanner$1);

export { CameraScanner };
